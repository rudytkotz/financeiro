[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "NODE_ENV=production node packages/backend/dist/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
